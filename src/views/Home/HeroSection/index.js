import React from 'react'
import {Container, Typography} from "@mui/material";

const HeroSection = () => {
    return (
        <Container>
            <Typography> Bienvenue sur l'app </Typography>
        </Container>
    );
};

export default HeroSection;